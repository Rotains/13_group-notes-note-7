#include <stdio.h>

void Swap(int a, int b);
int main(void)
{
	int x = 10, y = 5;
	printf("Swap() 함수 실행 전\n");
	printf("x = %d, y= %d이다.\n\n", x, y);

	Swap(x, y);
	printf("Swap() 함수 실행 후 \n");
	printf("x = %d, y = %d이다.\n", x, y);

	return 0;
}
void Swap(int a, int b);
{
	int temp;
	temp = a;
	a = b;
	b = temp;
}
결과 값이 바뀌지 않고 그대로 출력되었는데, 왜 그런지 대화해보자.

인자를 값으로 전달(call by value)
인자를 주소로 전달(call by reference)

301p
표 10-1 배열 a 주소 표현하는 방법.

#if 01
#include <stdio.h>

int SumArray(int* pA, int Size);
int main(void)
{
	int a[] = { 10,5,15,25,7 };
	int Sum;
	Sum = SumArray(a, 5);
	printf("배열의 합 : %d \n", Sum);

	return 0;
}

int SumArray(int* pA, int Size)
{
	int result = 0, i;
	for (i = 0; i < Size; i++)
		result += pA[i];

	return result;
}
#endif
> 혼자해보기 
